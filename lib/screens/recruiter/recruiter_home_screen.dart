import 'package:flutter/material.dart';

class RecruiterHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Recruiter Home')),
      body: Center(child: Text('Welcome, Recruiter!')),
    );
  }
}